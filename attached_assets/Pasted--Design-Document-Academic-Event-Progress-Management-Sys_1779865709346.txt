# Design Document
## Academic Event & Progress Management System

### 1. ARCHITECTURE OVERVIEW
┌─────────────────────────────────────────────────────────────┐
│ CLIENT LAYER │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ React SPA (TypeScript + Vite) │ │
│ │ - Components (Claymorphism UI) │ │
│ │ - State Management (Context API) │ │
│ │ - Routing (React Router) │ │
│ │ - HTTP Client (Axios) │ │
│ └──────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
↕ HTTPS
┌─────────────────────────────────────────────────────────────┐
│ API LAYER │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ Express.js REST API (TypeScript) │ │
│ │ - Route Handlers │ │
│ │ - Middleware (Auth, Validation, Error) │ │
│ │ - Controllers │ │
│ │ - Services (Business Logic) │ │
│ └──────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
↕
┌─────────────────────────────────────────────────────────────┐
│ DATA LAYER │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ │
│ │ PostgreSQL │ │ OpenAI API │ │ File Storage │ │
│ │ (Prisma ORM) │ │ (GPT-4) │ │ (Local/S3) │ │
│ └──────────────┘ └──────────────┘ └──────────────┘ │
└─────────────────────────────────────────────────────────────┘

text


---

### 2. SYSTEM COMPONENTS

#### 2.1 Frontend Architecture
src/
├── components/
│ ├── common/ # Reusable UI components
│ │ ├── Button.tsx
│ │ ├── Card.tsx
│ │ ├── Input.tsx
│ │ ├── Modal.tsx
│ │ └── ClayElement.tsx
│ ├── layout/ # Layout components
│ │ ├── Header.tsx
│ │ ├── Sidebar.tsx
│ │ └── Footer.tsx
│ ├── auth/ # Authentication components
│ │ ├── LoginForm.tsx
│ │ └── ProtectedRoute.tsx
│ ├── events/ # Event management
│ │ ├── EventList.tsx
│ │ ├── EventCard.tsx
│ │ ├── EventForm.tsx
│ │ └── EventCalendar.tsx
│ ├── progress/ # Progress tracking
│ │ ├── ProgressDashboard.tsx
│ │ ├── ProgressChart.tsx
│ │ └── ProgressForm.tsx
│ └── ai/ # AI features
│ ├── SummaryCard.tsx
│ ├── InsightsPanel.tsx
│ └── TrendChart.tsx
├── contexts/ # React Context
│ ├── AuthContext.tsx
│ ├── ThemeContext.tsx
│ └── EventContext.tsx
├── hooks/ # Custom hooks
│ ├── useAuth.ts
│ ├── useEvents.ts
│ └── useProgress.ts
├── services/ # API services
│ ├── api.ts
│ ├── authService.ts
│ ├── eventService.ts
│ └── aiService.ts
├── utils/ # Utilities
│ ├── dateHelpers.ts
│ ├── validators.ts
│ └── formatters.ts
├── types/ # TypeScript types
│ └── index.ts
├── styles/ # Global styles
│ └── clay.css
└── App.tsx

text


#### 2.2 Backend Architecture
src/
├── controllers/ # Request handlers
│ ├── authController.ts
│ ├── eventController.ts
│ ├── progressController.ts
│ └── aiController.ts
├── services/ # Business logic
│ ├── authService.ts
│ ├── eventService.ts
│ ├── progressService.ts
│ └── aiService.ts
├── middleware/ # Express middleware
│ ├── auth.ts
│ ├── validation.ts
│ ├── errorHandler.ts
│ └── rateLimiter.ts
├── models/ # Data models (Prisma)
│ └── schema.prisma
├── routes/ # API routes
│ ├── authRoutes.ts
│ ├── eventRoutes.ts
│ ├── progressRoutes.ts
│ └── aiRoutes.ts
├── utils/ # Utilities
│ ├── logger.ts
│ ├── jwt.ts
│ └── validators.ts
├── config/ # Configuration
│ ├── database.ts
│ ├── openai.ts
│ └── app.ts
└── server.ts # Entry point

text


---

### 3. DATABASE DESIGN

#### 3.1 Entity Relationship Diagram
┌─────────────────────┐
│ departments │
├─────────────────────┤
│ id (PK) │
│ name │
│ code │
│ head_user_id (FK) │
│ created_at │
│ updated_at │
└─────────────────────┘
│
│ 1:N
│
┌─────────────────────┐
│ users │
├─────────────────────┤
│ id (PK) │
│ email │
│ password_hash │
│ first_name │
│ last_name │
│ role │ ← ENUM: ADMIN, FACULTY, STUDENT, MANAGEMENT
│ department_id (FK) │
│ is_active │
│ last_login │
│ created_at │
│ updated_at │
└─────────────────────┘
│
│ 1:N
├──────────────┐
│ │
┌─────────────────────┐ │
│ events │ │
├─────────────────────┤ │
│ id (PK) │ │
│ title │ │
│ description │ │
│ category │ │ ← ENUM: ACADEMIC, EXTRACURRICULAR, ADMIN
│ status │ │ ← ENUM: PLANNED, ONGOING, COMPLETED, CANCELLED
│ priority │ │ ← ENUM: LOW, MEDIUM, HIGH
│ start_date │ │
│ end_date │ │
│ location │ │
│ created_by (FK) │─┘
│ department_id (FK) │
│ created_at │
│ updated_at │
└─────────────────────┘
│
│ 1:N
│
┌─────────────────────┐
│ event_attachments │
├─────────────────────┤
│ id (PK) │
│ event_id (FK) │
│ file_name │
│ file_path │
│ file_size │
│ mime_type │
│ uploaded_by (FK) │
│ uploaded_at │
└─────────────────────┘

┌─────────────────────┐
│ progress_logs │
├─────────────────────┤
│ id (PK) │
│ title │
│ description │
│ week_number │
│ year │
│ metrics (JSON) │
│ created_by (FK) │
│ department_id (FK) │
│ created_at │
│ updated_at │
└─────────────────────┘

┌─────────────────────┐
│ ai_summaries │
├─────────────────────┤
│ id (PK) │
│ type │ ← ENUM: WEEKLY, MONTHLY, PROGRESS, TREND
│ content (TEXT) │
│ insights (JSON) │
│ department_id (FK) │
│ date_range_start │
│ date_range_end │
│ generated_at │
└─────────────────────┘

┌─────────────────────┐
│ audit_logs │
├─────────────────────┤
│ id (PK) │
│ user_id (FK) │
│ action │
│ entity_type │
│ entity_id │
│ changes (JSON) │
│ ip_address │
│ created_at │
└─────────────────────┘

text


#### 3.2 Database Indexes

```sql
-- Users
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_department ON users(department_id);
CREATE INDEX idx_users_role ON users(role);

-- Events
CREATE INDEX idx_events_department ON events(department_id);
CREATE INDEX idx_events_created_by ON events(created_by);
CREATE INDEX idx_events_dates ON events(start_date, end_date);
CREATE INDEX idx_events_status ON events(status);
CREATE INDEX idx_events_category ON events(category);

-- Progress Logs
CREATE INDEX idx_progress_department ON progress_logs(department_id);
CREATE INDEX idx_progress_week ON progress_logs(week_number, year);

-- AI Summaries
CREATE INDEX idx_ai_summaries_department ON ai_summaries(department_id);
CREATE INDEX idx_ai_summaries_dates ON ai_summaries(date_range_start, date_range_end);

-- Audit Logs
CREATE INDEX idx_audit_user ON audit_logs(user_id);
CREATE INDEX idx_audit_created ON audit_logs(created_at);
4. API DESIGN
4.1 RESTful Endpoints
Authentication

text

POST   /api/auth/login
POST   /api/auth/logout
POST   /api/auth/refresh
POST   /api/auth/forgot-password
POST   /api/auth/reset-password
GET    /api/auth/me
Events

text

GET    /api/events                    # List events (with filters)
GET    /api/events/:id                # Get single event
POST   /api/events                    # Create event (Faculty only)
PUT    /api/events/:id                # Update event (Faculty only)
DELETE /api/events/:id                # Delete event (Faculty only)
GET    /api/events/calendar           # Calendar view
POST   /api/events/:id/attachments    # Upload attachment
DELETE /api/events/:id/attachments/:attachmentId
Progress

text

GET    /api/progress                  # List progress logs
GET    /api/progress/:id              # Get single log
POST   /api/progress                  # Create log (Faculty only)
PUT    /api/progress/:id              # Update log (Faculty only)
DELETE /api/progress/:id              # Delete log (Faculty only)
GET    /api/progress/stats            # Get statistics
AI Features

text

POST   /api/ai/generate-summary       # Generate summary
GET    /api/ai/summaries              # List summaries
GET    /api/ai/summaries/:id          # Get specific summary
POST   /api/ai/analyze-progress       # Analyze progress
GET    /api/ai/insights               # Get insights
Users

text

GET    /api/users                     # List users (Admin only)
GET    /api/users/:id                 # Get user
PUT    /api/users/:id                 # Update user
DELETE /api/users/:id                 # Delete user (Admin only)
POST   /api/users                     # Create user (Admin only)
Departments

text

GET    /api/departments               # List departments
GET    /api/departments/:id           # Get department
POST   /api/departments               # Create (Admin only)
PUT    /api/departments/:id           # Update (Admin only)
4.2 Request/Response Examples
Create Event

TypeScript

// POST /api/events
Request:
{
  "title": "Annual Tech Symposium",
  "description": "Department-wide technical symposium...",
  "category": "ACADEMIC",
  "status": "PLANNED",
  "priority": "HIGH",
  "startDate": "2024-03-15T09:00:00Z",
  "endDate": "2024-03-15T17:00:00Z",
  "location": "Main Auditorium"
}

Response: 201 Created
{
  "success": true,
  "data": {
    "id": "clx123...",
    "title": "Annual Tech Symposium",
    "category": "ACADEMIC",
    "status": "PLANNED",
    "createdBy": {
      "id": "user123",
      "name": "Dr. Sarah Johnson"
    },
    "createdAt": "2024-03-01T10:00:00Z"
  }
}
Generate AI Summary

TypeScript

// POST /api/ai/generate-summary
Request:
{
  "type": "WEEKLY",
  "dateRangeStart": "2024-03-01",
  "dateRangeEnd": "2024-03-07"
}

Response: 200 OK
{
  "success": true,
  "data": {
    "id": "sum123",
    "type": "WEEKLY",
    "content": "This week saw significant progress with 12 events completed...",
    "insights": {
      "totalEvents": 12,
      "completionRate": 0.92,
      "topCategory": "ACADEMIC",
      "recommendations": [
        "Consider increasing extracurricular activities",
        "High student engagement in technical workshops"
      ]
    },
    "generatedAt": "2024-03-08T00:00:00Z"
  }
}
4.3 Error Responses
TypeScript

// Standard error format
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid input data",
    "details": [
      {
        "field": "email",
        "message": "Invalid email format"
      }
    ]
  }
}

// Error codes
- VALIDATION_ERROR (400)
- UNAUTHORIZED (401)
- FORBIDDEN (403)
- NOT_FOUND (404)
- CONFLICT (409)
- RATE_LIMIT_EXCEEDED (429)
- INTERNAL_ERROR (500)
5. SECURITY DESIGN
5.1 Authentication Flow
text

┌──────┐                 ┌──────┐                 ┌──────┐
│Client│                 │ API  │                 │  DB  │
└──┬───┘                 └──┬───┘                 └──┬───┘
   │                        │                        │
   │ POST /auth/login       │                        │
   │ {email, password}      │                        │
   ├───────────────────────>│                        │
   │                        │ Query user             │
   │                        ├───────────────────────>│
   │                        │                        │
   │                        │ User data              │
   │                        │<───────────────────────┤
   │                        │                        │
   │                        │ Verify password        │
   │                        │ (bcrypt.compare)       │
   │                        │                        │
   │                        │ Generate JWT           │
   │                        │ (accessToken + refresh)│
   │                        │                        │
   │ {tokens, user}         │                        │
   │<───────────────────────┤                        │
   │                        │                        │
   │ Store tokens           │                        │
   │ (httpOnly cookies)     │                        │
   │                        │                        │
   │ GET /api/events        │                        │
   │ Header: Authorization  │                        │
   ├───────────────────────>│                        │
   │                        │ Verify JWT             │
   │                        │ Extract user           │
   │                        │                        │
   │                        │ Query events           │
   │                        ├───────────────────────>│
   │                        │                        │
   │ Events data            │                        │
   │<───────────────────────┤                        │
5.2 Authorization Matrix
Endpoint	Student	Faculty	Admin	Management
GET /events	✅	✅	✅	✅
POST /events	❌	✅	✅	❌
PUT /events/:id	❌	✅ (own)	✅	❌
DELETE /events/:id	❌	✅ (own)	✅	❌
GET /progress	✅	✅	✅	✅
POST /progress	❌	✅	✅	❌
POST /ai/generate	❌	✅	✅	✅
GET /users	❌	❌	✅	✅
POST /users	❌	❌	✅	❌
5.3 Security Measures
Password Security

bcrypt hashing (12 rounds)
Minimum 8 characters
Complexity requirements enforced
JWT Security

Short-lived access tokens (15 min)
Long-lived refresh tokens (7 days)
Token rotation on refresh
Blacklist for revoked tokens
Input Validation

Zod schemas for all inputs
Sanitization of user inputs
File upload validation (type, size)
Rate Limiting

Login: 5 attempts per 15 min
API: 100 requests per 15 min
AI: 10 requests per hour
CORS Configuration

Whitelist specific origins
Credentials allowed for same-origin
SQL Injection Prevention

Prisma ORM with parameterized queries
No raw SQL queries
6. CLAYMORPHISM UI DESIGN SYSTEM
6.1 Color Palette
CSS

:root {
  /* Primary Colors */
  --clay-lavender: #E0BBE4;
  --clay-lavender-light: #F0DFF3;
  --clay-lavender-dark: #D1A5D6;
  
  /* Secondary Colors */
  --clay-mint: #C7EAE4;
  --clay-mint-light: #E0F7F4;
  --clay-mint-dark: #A8DDD4;
  
  /* Accent Colors */
  --clay-blue: #A8D8EA;
  --clay-blue-light: #C5E7F3;
  --clay-blue-dark: #8BC9DD;
  
  /* Neutral Colors */
  --clay-cream: #FAF4F1;
  --clay-white: #FFFFFF;
  --clay-gray-light: #F5F0ED;
  --clay-gray: #E8DFD8;
  --clay-charcoal: #3D3D3D;
  
  /* Semantic Colors */
  --clay-success: #B8E6D5;
  --clay-warning: #FFE8CC;
  --clay-error: #FFD4D4;
  --clay-info: #D4E4FF;
  
  /* Shadows */
  --shadow-outer: 8px 8px 24px rgba(163, 177, 198, 0.3),
                  -8px -8px 24px rgba(255, 255, 255, 0.8);
  --shadow-inner: inset 4px 4px 12px rgba(163, 177, 198, 0.2),
                  inset -4px -4px 12px rgba(255, 255, 255, 0.9);
  --shadow-hover: 12px 12px 32px rgba(163, 177, 198, 0.35),
                  -12px -12px 32px rgba(255, 255, 255, 0.85);
}
6.2 Component Styles
Clay Card

CSS

.clay-card {
  background: var(--clay-white);
  border-radius: 20px;
  padding: 24px;
  box-shadow: var(--shadow-outer);
  transition: all 0.3s ease;
}

.clay-card:hover {
  box-shadow: var(--shadow-hover);
  transform: translateY(-2px);
}
Clay Button

CSS

.clay-button {
  background: linear-gradient(145deg, var(--clay-lavender-light), var(--clay-lavender));
  border: none;
  border-radius: 16px;
  padding: 12px 28px;
  box-shadow: var(--shadow-outer);
  cursor: pointer;
  font-weight: 600;
  transition: all 0.2s ease;
}

.clay-button:hover {
  box-shadow: var(--shadow-hover);
  transform: scale(1.02);
}

.clay-button:active {
  box-shadow: var(--shadow-inner);
  transform: scale(0.98);
}
Clay Input

CSS

.clay-input {
  background: var(--clay-gray-light);
  border: none;
  border-radius: 16px;
  padding: 14px 20px;
  box-shadow: var(--shadow-inner);
  font-size: 16px;
  transition: all 0.2s ease;
}

.clay-input:focus {
  outline: none;
  box-shadow: var(--shadow-inner),
              0 0 0 3px var(--clay-lavender-light);
}
6.3 Layout Principles
Generous Spacing: 24-32px between major sections
Soft Corners: All elements have 16-20px border radius
Layered Depth: Use shadows to create hierarchy
Gentle Animations: 200-300ms transitions
Pastel Backgrounds: Cream base with colorful accents
7. PERFORMANCE OPTIMIZATION
7.1 Frontend Optimization
Code Splitting: Route-based lazy loading
Asset Optimization:
Image compression (WebP format)
SVG for icons
CSS purging (unused styles removed)
Caching:
Service Worker for offline support
React Query for data caching
Browser cache headers
Bundle Size: <200KB initial load
7.2 Backend Optimization
Database:
Connection pooling
Query optimization
Proper indexing
Pagination (limit 50 items per page)
Caching:
Redis for session storage
API response caching (5-minute TTL)
Static file caching
Compression: Gzip/Brotli for responses
7.3 Performance Metrics
Time to First Byte (TTFB): <200ms
First Contentful Paint (FCP): <1.5s
Largest Contentful Paint (LCP): <2.5s
Time to Interactive (TTI): <3.5s
Cumulative Layout Shift (CLS): <0.1
8. DEPLOYMENT ARCHITECTURE
text

┌─────────────────────────────────────────────────┐
│                  Load Balancer                   │
│                   (Nginx/AWS ALB)                │
└───────────────────┬─────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
┌───────▼──────┐        ┌───────▼──────┐
│  App Server  │        │  App Server  │
│  (Node.js)   │        │  (Node.js)   │
│  Port 3000   │        │  Port 3000   │
└───────┬──────┘        └───────┬──────┘
        │                       │
        └───────────┬───────────┘
                    │
        ┌───────────▼───────────┐
        │                       │
┌───────▼──────┐        ┌───────▼──────┐
│  PostgreSQL  │        │    Redis     │
│  (Primary)   │        │   (Cache)    │
└───────┬──────┘        └──────────────┘
        │
┌───────▼──────┐
│  PostgreSQL  │
│  (Replica)   │
└──────────────┘
8.1 Infrastructure Components
Web Server: Nginx (reverse proxy, SSL termination)
Application Server: Node.js (2+ instances)
Database: PostgreSQL 15+ (with replication)
Cache: Redis 7+
File Storage: AWS S3 or local filesystem
Monitoring: Prometheus + Grafana
Logging: Winston + ELK Stack
9. MONITORING & OBSERVABILITY
9.1 Logging Strategy
TypeScript

// Log levels
- ERROR: Application errors, exceptions
- WARN: Deprecated features, unusual activity
- INFO: General application flow
- DEBUG: Detailed diagnostic information

// Log format (JSON)
{
  "timestamp": "2024-03-01T10:00:00Z",
  "level": "INFO",
  "service": "api",
  "message": "User logged in",
  "userId": "user123",
  "ip": "192.168.1.1",
  "requestId": "req-abc123"
}
9.2 Metrics to Track
Application Metrics:

Request rate (requests/min)
Error rate (%)
Response time (p50, p95, p99)
Active users
Business Metrics:

Events created per day
User logins per day
AI summaries generated
File uploads
Infrastructure Metrics:

CPU usage
Memory usage
Database connections
Disk I/O
9.3 Alerting Rules
CPU > 80% for 5 minutes
Memory > 85% for 5 minutes
Error rate > 5% for 2 minutes
Response time p95 > 1s for 5 minutes
Database connection pool exhausted